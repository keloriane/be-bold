<section class="product with-padding">
  <div class="title-container">
    <h2 class="sf blue">Nos activités</h2>
  </div>
  <div class="product-list-container">
    {% for post in product %}
      <a href="{{ post.link }}" data-color="#b3a8b3" data-imagelarge="{{ Image(post.custom.thumbnail).src }}"
         data-imagesmall="{{ Image(post.custom.thumbnail).src }}">{{ post.title }}</a>
    {% endfor %}
  </div>
  <div class="portfolio__image--l">
    <div class="image_inside">

    </div>
  </div>
</section>

<!-- /Portfolio -->
